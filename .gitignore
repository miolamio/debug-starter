node_modules
.next
out
*.log
.DS_Store

# В реальных проектах .env*.local НЕ коммитят. Здесь — учебный
# репозиторий, и .env.local с опечаткой и есть тот баг,
# который студент диагностирует. Поэтому он в git.
